# Expo inlines EXPO_PUBLIC_* into the JS bundle at build time.
# Anything here is readable by anyone with the app. Never put a secret in this file.

# Base URL of the Next.js API (apps/web). Use your LAN IP for a device on dev.
EXPO_PUBLIC_API_URL=http://localhost:3000

EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxx
# No Stream key here on purpose: POST /api/stream/token returns the API key
# alongside the user token, so there is one source of truth and one place to
# rotate it.
# Sentry DSN is public by design. The build-time SENTRY_AUTH_TOKEN that uploads
# source maps is NOT — keep it out of this file; set it as an EAS secret.
EXPO_PUBLIC_SENTRY_DSN=
